# 🚀 تفعيل التحديث — دليل مختصر بالعربي

هذا الدليل يشرح **بالضبط** شو تعمل بعد دمج هذا التحديث.
كل خطوة مكتوبة للمرة الوحدة — اتبعها بالترتيب وخلاص.

---

## أولاً: قفل قاعدة البيانات (مهم — أول شي)

بدون هالخطوة، قاعدة البيانات مفتوحة لأي حد عنده الرابط (وهو منشور بالريبو العام).
**هالملف ما بيحذف أي بيانات — بس بيضبط الصلاحيات ويضيف أعمدة ناقصة.**

1. افتح [supabase.com](https://supabase.com) → ادخل على مشروع Pro House.
2. من القائمة اليسار: **SQL Editor** → **New query**.
3. افتح ملف **`supabase_hardening.sql`** (بجذر هذا الريبو)، انسخ **كل** محتواه، والصقه بالمحرر.
4. اضغط **Run** (زر أخضر تحت).
   - المتوقع: يطلع "Success. No rows returned" — طبيعي.
   - لو طلع **خطأ أحمر** (ERROR) → وقف عنك وابعث نص الخطأ.
5. بعد ما ينجح: نفّذ هالسطر وانسخ الناتج (بتحتاجه بالخطوة التالية):

   ```sql
   select value from settings where key = 'integrationToken';
   ```

   الناتج عبارة عن سلسلة طويلة — انسخها كلها.

## ثانياً: ربط سكربت مبيعات تابسنس بالنظام الجديد

(عشان لما السكربت يسحب مبيعات تابسنس، توصل للنظام الجديد كمان — الشيت بيضل يتحدث عادي.)

6. روح للريبو على GitHub → **Settings** (فوق) → **Secrets and variables** → **Actions**.
7. اضغط على الاسم `TABSENSE_CONFIG_JSON` → **Update**.
8. داخل النص، ضيف سطرين جديدين (انتبه للفواصل `,`):

   ```json
   "supabaseUrl": "https://sadtinfdwucwrxlmwxov.supabase.co",
   "supabaseToken": "الصق هنا التوكن اللي نسخته من الخطوة 5"
   ```

9. اضغط **Save / Update secret**.

## ثالثاً: دمج الكود

10. ادمج طلب المراجعة هذا (pr الإصلاحات)، بعدها طلب "الهجرة إلى Supabase".
11. افتح الموقع وحدّثه تحديث قوي:
    - كمبيوتر: **Ctrl + Shift + R**
    - جوال: سكّر التاب من قائمة التابات وافتح الموقع من جديد.

## رابعاً: تجربة سريعة (5 دقايق)

12. سجّل دخول بأي رقم سري **قديم** (نفس الأرقام تشتغل).
    - ملاحظة: كل الموظفين رح يسجلوا دخول **مرة وحدة** من جديد — الجلسات القديمة بتبطل.
13. جرّب الحالات إلي كانت مكسورة قبل — المفروض هلق كلها تمام:
    - احفظ تقرير الاستلام ← بعدين **عدّل رقم وحفظ مرة تانية** (لازم يتحدث عادي بدون بطء).
    - كمّل تقرير المتبقي ← ارجع لشاشة الاستلام (لازم أرقام الاستلام **تضل زي ما هي**).
    - طلبية الغد: شيل صنف واحفظ ← افتحها من جديد (لازم الصنف المشال **ما يرجع**).
14. إذا كل شي تمام 🎉 — السرعة صارت بالملي ثانية، والأمان مقفل صح.

---

## لسّا ناقص (المرحلة الجاية — مو عاجل)

- **إشعارات الواتساب التلقائية** (تنبيه الإرجاع المرتفع / إشعار الشيف بطلبية الغد / الملخص اليومي) — لساتها بالنظام القديم، فبتبطل تشتغل مع النظام الجديد. تحتاج نقل للنظام الجديد.
- **نسخة احتياطية أسبوعية** لقاعدة البيانات الجديدة (ملف CSV/JSON) — الشيت القديم بضل يتاخذ نسخة منه تلقائياً، بس الجديد لسا بلا باك أب.
- **نقل البيانات التاريخية** من الشيت القديم للتطبيق الجديد (اختياري — بيصير لاحقاً إذا بدكم تقارير عن الشهور القديمة جوا التطبيق).
- **زر "إغلاق اليوم"** فعلاً بيوصل للنظام الجديد (حالياً بس محلي).

## إذا واجهت أي مشكلة

- التطبيق يبيّن شاشة فاضية بعد الدخول؟ → حدّث تحديث قوي (الخطوة 11).
- رد "رقم سري غير صحيح" لكل الموظفين؟ → تأكد إنك نفّذت ملف `supabase_hardening.sql` (الخطوة 4) وتقول للموظفين يسجلوا دخول من جديد.
- أي رسالة خطأ تانية → صوّرها وابعتها.
